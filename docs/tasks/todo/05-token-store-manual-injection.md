# Задача 05 — TokenStore + manual injection

## Цель
Добавить базовый безопасный механизм хранения токенов и ручной инъекции для dev-сценариев.

## Инструкция
- Реализовать in-memory `ITokenStore`.
- Поддержать ручную загрузку токенов (manual injection).
- Добавить тесты на set/get/expiry.
- После завершения задачи и мержа PR перенести этот файл из `docs/tasks/todo/` в `docs/tasks/done/`.

## Критерии приемки
- Токены корректно сохраняются и извлекаются.
- Поведение с истечением срока действия покрыто тестами.
- `dotnet test MultiLlm.slnx` проходит.
