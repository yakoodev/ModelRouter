# Задача 09 — Image/File pipeline

## Цель
Обеспечить end-to-end прохождение `ImagePart` и `FilePart` через core и провайдеры.

## Инструкция
- Реализовать обработку `ImagePart` минимум в 2 провайдерах.
- Реализовать передачу `FilePart` в совместимых провайдерах.
- Добавить тесты на ограничения размеров и корректный маппинг вложений.
- После завершения задачи и мержа PR перенести этот файл из `docs/tasks/todo/` в `docs/tasks/done/`.

## Критерии приемки
- `ImagePart` и `FilePart` проходят end-to-end.
- Есть тесты на ограничения размеров/форматов.
- `dotnet test MultiLlm.slnx` проходит.
